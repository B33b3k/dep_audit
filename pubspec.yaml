
name: dep_audit
description: A comprehensive Dart and Flutter package dependency audit tool that helps identify outdated packages, security vulnerabilities, and provides actionable insights to maintain healthy project dependencies.
version: 0.1.8
homepage: https://pub.dev/packages/dep_audit
repository: https://github.com/B33b3k/dep_audit
issue_tracker: https://github.com/B33b3k/dep_audit/issues
environment:
  sdk: '>=3.0.0 <4.0.0'

platforms:
  linux:
  macos:
  windows:
  web:

dependencies:
  args: ^2.4.2
  glob: ^2.1.2
  http: ^1.2.1
  path: ^1.9.0
  pub_semver: ^2.1.4
  yaml: ^3.1.2

dev_dependencies:
  coverage: ^1.6.0
  test: ^1.24.0

executables:
  dep_audit:
